{
  "name": "admin-esxape-ride",
  "private": true,
  "packageManager": "pnpm@9.11.0",
  "engines": {
    "node": ">19 20<"
  },
  "volta": {
    "node": ">=19 <20",
    "pnpm": "9.11.0"
  },
  "scripts": {
    "vercel:info": "node tools/vercel-info.mjs",
    "dev": "turbo run dev --parallel",
    "build": "node tools/offline-pnpm.mjs --filter game-web build",
    "build:standard": "pnpm --filter game-web build",
    "build:turbo": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
