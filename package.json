{
  "name": "admin-esxape-ride",
  "private": true,
  "packageManager": "yarn@4.9.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": "22.x"
  },
  "volta": {
    "node": "22.11.0",
    "yarn": "4.9.4"
  },
  "scripts": {
    "vercel:info": "node tools/vercel-info.mjs",
    "dev": "turbo run dev --parallel",
    "build": "npm --prefix apps/admin run build && npm --prefix apps/game-web run build",
    "build:admin": "npm --prefix apps/admin run build",
    "build:game": "npm --prefix apps/game-web run build",
    "build:turbo": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
