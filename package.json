{
  "name": "esxape-ride",
  "private": true,
  "engines": {
    "node": ">=20.18.0 <21.0.0"
  },
  "packageManager": "pnpm@9.12.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "build:game": "node tools/offline-pnpm.mjs --filter game-web build"
  },
  "devDependencies": {
    "turbo": "^2.1.0"
  }
}
