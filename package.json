{
  "name": "esxape-ride",
  "private": true,
  "packageManager": "pnpm@9.12.1",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "vercel": "NODE_OPTIONS=--require ./tools/urlsearchparams-shim.cjs vercel"
  },
  "devDependencies": {
    "turbo": "^2.1.0"
  }
}
